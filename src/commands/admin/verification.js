import { SlashCommandBuilder, PermissionFlagsBits, ChannelType, MessageFlags } from 'discord.js'; import { updateConfig } from '../../modules/community/store.js'; import { verifyPanel, success } from '../../modules/community/ui.js';
export default { data:new SlashCommandBuilder().setName('verification').setDescription('Configure member verification').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addSubcommand(s=>s.setName('setup').setDescription('Create verification panel').addChannelOption(o=>o.setName('channel').setDescription('Panel channel').addChannelTypes(ChannelType.GuildText).setRequired(true)).addRoleOption(o=>o.setName('role').setDescription('Role after verification').setRequired(true))).addSubcommand(s=>s.setName('disable').setDescription('Disable verification')), async execute(i,client){const sub=i.options.getSubcommand(); if(sub==='disable'){await updateConfig(client,i.guildId,{verification:{enabled:false}});return i.reply({...success('אימות','מערכת האימות כובתה.'),flags:MessageFlags.Ephemeral});} const channel=i.options.getChannel('channel'),role=i.options.getRole('role'); await updateConfig(client,i.guildId,{verification:{enabled:true,channelId:channel.id,roleId:role.id}}); await channel.send(verifyPanel()); await i.reply({...success('אימות','לוח האימות נשלח והמערכת הופעלה.'),flags:MessageFlags.Ephemeral});} };
