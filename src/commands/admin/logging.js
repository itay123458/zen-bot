import { SlashCommandBuilder, PermissionFlagsBits, ChannelType, MessageFlags } from 'discord.js'; import { updateConfig } from '../../modules/community/store.js'; import { success } from '../../modules/community/ui.js';
export default { data:new SlashCommandBuilder().setName('logging').setDescription('Configure server logs').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addSubcommand(s=>s.setName('setup').setDescription('Set the log channel').addChannelOption(o=>o.setName('channel').setDescription('Log channel').addChannelTypes(ChannelType.GuildText).setRequired(true))).addSubcommand(s=>s.setName('disable').setDescription('Disable server logs')),async execute(i,client){const s=i.options.getSubcommand(),ch=i.options.getChannel('channel');await updateConfig(client,i.guildId,{logging:s==='disable'?{enabled:false}:{enabled:true,channelId:ch.id}});await i.reply({...success('לוגים',s==='disable'?'מערכת הלוגים כובתה.':`הלוגים יישלחו אל ${ch}.`),flags:MessageFlags.Ephemeral});} };
